language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"

script: nosetests

install:
  - "pip install -r requirements.txt

notifications:
  email:
    - komissarex@gmail.com

services: postgresql
before_script:
  - psql -c 'create database test;' -U postgres
